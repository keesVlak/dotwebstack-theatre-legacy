spring:
  jersey:
    type: filter

logging.level:
  org.dotwebstack.framework: DEBUG

dotwebstack:
  config:
    resourcePath: file:/opt/config
    systemGraph: http://dotwebstack.org/configuration/SandboxTheatre
